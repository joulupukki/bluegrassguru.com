---
const screenshots = [
  { src: "/images/screenshot-library.svg", alt: "Song library view" },
  { src: "/images/screenshot-song.svg", alt: "Song detail with chords and lyrics" },
  { src: "/images/screenshot-setlist.svg", alt: "Set list view" },
  { src: "/images/screenshot-search.svg", alt: "Web search for new songs" },
];
---

<section id="screenshots" class="bg-surface py-20 sm:py-28">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <div class="mb-14 text-center">
      <h2 class="mb-4 font-heading text-3xl font-bold sm:text-4xl">
        See It in Action
      </h2>
      <p class="mx-auto max-w-2xl text-text-light">
        A clean, intuitive interface designed for musicians — not techies.
      </p>
    </div>

    <div class="flex gap-6 overflow-x-auto pb-6 snap-x snap-mandatory sm:justify-center sm:flex-wrap sm:overflow-x-visible sm:pb-0">
      {
        screenshots.map((s) => (
          <div class="flex-none snap-center w-56 sm:w-52 lg:w-56">
            <!-- iPhone frame -->
            <div class="rounded-[2rem] border-4 border-heading bg-heading p-2 shadow-xl">
              <div class="overflow-hidden rounded-[1.5rem] bg-white">
                <img
                  src={s.src}
                  alt={s.alt}
                  width="280"
                  height="560"
                  class="w-full"
                  loading="lazy"
                />
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
